import { async, ComponentFixture, TestBed } from '@angular/core/testing';

import { NiceConfirmComponent } from './nice-confirm.component';

describe('NiceConfirmComponent', () => {
  let component: NiceConfirmComponent;
  let fixture: ComponentFixture<NiceConfirmComponent>;

  beforeEach(async(() => {
    TestBed.configureTestingModule({
      declarations: [ NiceConfirmComponent ]
    })
    .compileComponents();
  }));

  beforeEach(() => {
    fixture = TestBed.createComponent(NiceConfirmComponent);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
